#include "monty.h"
/**
 * print_topstack - prints the top
 * @head: stack head
 * @count: line_number
 * Return: no return
*/
void print_topstack(stack_t **head, unsigned int count)
{
	(void)head;
	(void)count;
	bus.lifi = 0;
}
